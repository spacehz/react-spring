apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: product-app
  labels:
    app: product-app
    component: mysql
spec:
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
  selector:
    app: product-app
    component: mysql
